"""
Admin configuration for Mobile API
This app doesn't use Django admin, all administration is done through the platform admin
"""
from django.contrib import admin

# Mobile API is managed through REST endpoints
# No admin models needed here
